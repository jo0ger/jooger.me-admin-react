@import '../../styles/vars/index'

$bread-margin         = $body-padding
$bread-padding        = $body-padding

.bread {
  height: 48px;
  line-height: 48px;
  margin-top: $bread-margin
  margin-bottom: (0 - $bread-margin / 2);
  padding: 0 $bread-padding;

  i {
    margin-right: 5px;
  }

}
